management:
  security:
    enabled: false

spring.application.name: spcp-mdo-scoring

---

spring:
  profiles: dev
  cloud:
    config: 
      uri: https://sit-interlock.anthem.com/spcp-config-server
      
encrypt:
  key-store:
    location: file:${key.path}
    password: anthem$$docker
    alias: docker
    secret: anthem$$docker
            
---

spring:
  profiles: sit
  cloud:
    config: 
      uri: https://sit-interlock.anthem.com/spcp-config-server

encrypt:
  key-store:
    location: file:${key.path}
    password: anthem$$docker
    alias: docker
    secret: anthem$$docker
---

spring:
  profiles: uat
  cloud:
    config: 
      uri: https://uat-interlock.anthem.com/spcp-config-server

encrypt:
  key-store:
    location: file:${key.path}
    password: anthem$$docker
    alias: docker
    secret: anthem$$docker
 
---

spring:
  profiles: prod
  cloud:
    config: 
      uri: https://prod-interlock.anthem.com/spcp-config-server

encrypt:
  key-store:
    location: file:${key.path}
    password: anthem$$docker
    alias: docker
    secret: anthem$$docker
---
spring:
  profiles: perf
  cloud:
    config: 
      uri: https://uat-interlock.anthem.com/perf/spcp-config-server

encrypt:
  key-store:
    location: file:${key.path}
    password: anthem$$docker
    alias: docker
    secret: anthem$$docker