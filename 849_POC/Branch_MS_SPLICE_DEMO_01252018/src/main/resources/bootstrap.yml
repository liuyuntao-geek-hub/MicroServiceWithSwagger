management:
    security:
        enabled: false
logging:
    level: DEBUG
springfox:
    documentation:
        swagger:
            v2:
                path: /api-docs
server:
    port: 8080
    contextPath: /springbootapp
spring:
    application:
        name: splice-service
    cloud:
        config: 
            uri: https://sit-interlock.anthem.com/utlconfig  
    datasource:
        driver-class-name: com.splicemachine.db.jdbc.ClientDriver
        url: jdbc:splice://dwbdtest1r2w9.wellpoint.com:1527/splicedb
        username: splice
        password: admin
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                dialect: org.hibernate.dialect.DerbyDialect

---

spring:
    profiles: dev

---

spring:
    profiles: sit

---

spring:
    profiles: uat

---

spring:
    profiles: prod
